node_modules
dist
.DS_Store
*.log
.env
.env.*

# Local reference only – CV source files, not for repo
references/

# Assets are in public/ only (used by build); ignore root media to avoid duplicates
/media/